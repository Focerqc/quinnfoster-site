const THREE = require('./three.js'); global.THREE = THREE; require('./STLLoader.js'); const htmlString = '<html><body>404 Not Found</body></html>'; const buffer = new Uint8Array(Buffer.from(htmlString)).buffer; try { const loader = new THREE.STLLoader(); const geo = loader.parse(buffer); console.log('Geometry:', geo); geo.center(); geo.computeVertexNormals(); console.log('Success'); } catch (e) { console.log('THROWN:', e); }
